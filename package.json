{
  "name": "ai-interview-system",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "php -d upload_max_filesize=250M -d post_max_size=250M -d memory_limit=256M -S 0.0.0.0:3000 router.php",
    "build": "php -l index.php && php -l includes/db.php && php -l admin/dashboard.php && php -l candidate/interview.php && php -l api/process.php",
    "start": "php -d upload_max_filesize=250M -d post_max_size=250M -d memory_limit=256M -S 0.0.0.0:3000 router.php",
    "lint": "php -l index.php && php -l includes/db.php"
  }
}
